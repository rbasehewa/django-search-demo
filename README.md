# Django Search Demo

Small Django 5.x project with a `vaccination` app and a `/vaccination/covid19/` page.

## Quick start
```bash
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
python manage.py runserver
